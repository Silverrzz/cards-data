{
    "schema_version": 1,
    "description": "Allowed top-level keys for each data/content/mods/<mod_id>/data.json.",
    "fields": [
        {
            "name": "name",
            "description": "Mod type id; must match the parent folder name.",
            "type": "string",
            "required": true
        },
        {
            "name": "description",
            "description": "What this mod does when used on a card.",
            "type": "string",
            "required": true
        },
        {
            "name": "attributes",
            "description": "Schema for attributes card JSON may supply for this mod type.",
            "type": "mod_attribute_definitions",
            "required": true
        }
    ]
}
